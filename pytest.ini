[pytest]
# test_email.py and test_transcribe.py in the project root are manual diagnostic
# scripts, not pytest tests: importing them calls the live APIs and sends mail.
# Collection is confined to tests/ so a bare `pytest -q` cannot trigger them.
testpaths = tests
